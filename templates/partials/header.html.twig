<nav class="navbar navbar-expand-lg navbar-light bg-light fixed-top shadow border-0">
    <div class="container">
        <a class="nav-link maj" href="{{ path ('app') }}"><img src="{{ asset('M2SP.png') }}" alt="logo"></a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation"><span class="navbar-toggler-icon"></span></button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
			<ul class="navbar-nav me-auto maj">
				<li class="nav-item">
					<a class="nav-link maj" href="{{ path ('app') }}"><strong>Accueil</strong></a>
				</li>
                <li class="nav-item">
					<a class="nav-link" href="{{ path ('unes_index') }}"><strong>News</strong></a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{ path ('articles_index') }}"><strong>Articles</strong></a>
				</li>
                <li class="nav-item">
					<a class="nav-link" href="{{ path ('recettes_index') }}"><strong>Recettes</strong></a>
				</li>
                <li class="nav-item">
					<a class="nav-link" href="{{ path ('apropos') }}"><strong>A propos</strong></a>
				</li>
			</ul>
            <ul class="navbar-nav ml-auto">
                {% if app.user %}
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                            {{app.user.firstName}} {{app.user.lastName}}
                        </a>
                        <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
                            <a href="{{ path ('une_new')}}" class="dropdown-item">Créer une news</a>
                            <a href="{{ path ('article_new')}}" class="dropdown-item">Créer un article</a>
                            <a href="{{ path ('recette_new')}}" class="dropdown-item">Créer une recette</a>
                            <div class="dropdown-divider"></div>
                            <a href="{{ path ('security_account' )}}" class="dropdown-item">Mon compte</a>
                            <a href="{{ path ('security_profile' ) }}" class="dropdown-item">Modifier mon profil</a>
                            <div class="dropdown-divider"></div>
                            {% if is_granted ('ROLE_ADMIN') %}
                                <a href="{{ path ('admin')}}" class="dropdown-item">Admin</a>
                            {% endif %}
                            <div class="dropdown-divider"></div>
                            <a href="{{ path ('app_logout')}}" class="dropdown-item">Déconnexion</a>
                        </div>
                    </li>
                {% else %}
					<li class="nav-item">
                        <a href="{{ path('app_register') }}" class="nav-link">Inscription</a>
                    </li>
                    <li class="nav-item">
                        <a href="{{ path('app_login') }}" class="nav-link">Connexion</a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>

<!-- Flash -->
{% for label, messages in app.flashes %}
	<div class="container pt-1">
		<div class="alert alert-{{ label }}">
			{% for message in messages %}
				<p>{{ message | raw }}</p>
			{% endfor %}
		</div>
	</div>
{% endfor %}
<!-- End Flash -->